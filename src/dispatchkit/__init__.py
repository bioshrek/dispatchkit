"""dispatchkit dispatch tooling (see docs/automation_task_dispatch_requirements.md).

D1 ships the task graph schema and validator: `model` (pure value objects),
`parse` (strict TOML → graph), `validate` (semantic invariants), `cli`
(`validate`, the command with the binary exit code).
"""
